#include <bits/stdc++.h>

map<int, string> PCbincmd;
long long str2num(std::string numstr);
bool* Dec2Bin(long long num, size_t size);
bool generateBinCmd();
void update_unfinished_cmd();
void getBinCmd()